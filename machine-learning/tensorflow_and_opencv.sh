sudo apt-get update
sudo apt-get install libatlas-base-dev
sudo pip3 install tensorflow opencv-python

#Next, you'll need to download the pre-trained model. You can use the "PoseNet" model, which is a popular model for human pose estimation
